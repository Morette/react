import React from 'react';

import closeIcon from '../../images/close.svg';

const IconClose = () => (
  <img src={closeIcon} alt="Close Icon" width={20} className="icon-close"/>
);


export default IconClose;